import { ClassStruct } from "./structs/ClassStruct";

function d2StringOfObject(
    o: object,
    name: string
): ClassStruct {
    // for (let entry of Object.entries(o)) {
    // }
    return new ClassStruct("nn");
}

export { d2StringOfObject };
